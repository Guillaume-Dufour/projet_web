var express = require('express');
var router = express.Router();
/*var users = require('./usersCtrlV2');

router.use('/users', require('./users'));

router.route('/users/inscription').get(users.inscription);

module.exports = router;*/
